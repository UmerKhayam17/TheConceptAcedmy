# Stateless InsightFace worker (no DB). From this folder:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#   uvicorn app:app --host 127.0.0.1 --port 8090
#
# School Node API stores faces + attendance in MongoDB only.
